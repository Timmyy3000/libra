{
  "name": "libra",
  "private": true,
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*",
    "trigger"
  ],
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter @libra/shared typecheck && bun run --filter @libra/trigger typecheck && bun run --filter web build",
    "start": "bun run --filter web start",
    "lint": "bun run --filter @libra/shared typecheck && bun run --filter @libra/trigger typecheck && bun run --filter web lint",
    "test": "bun run --filter @libra/shared test && bun run --filter @libra/trigger test",
    "typecheck": "bun run --filter @libra/shared typecheck && bun run --filter @libra/trigger typecheck && bun run --filter web typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "convex": "^1.36.0"
  }
}
